# Jupyter VM
A Vagrant VM to set up a data science environment easily.

## Prerequisits

- Vagrant
- vagrant-hostmaster
- VirtualBox

## Run

`vagrant up`
`vagrant ssh`
`./run_jupyter.sh`

Open databriefing.vm in your browser.
